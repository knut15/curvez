# 디자인 토큰

정본은 `apps/handwork/src/app/globals.css` 다. 이 표는 그 파일의 값을 **읽기 쉬운 hex 로 옮긴 사본**이고,
값이 갈리면 CSS 가 이긴다. 원본은 `oklch()` 로 적혀 있다.

## 이름 규칙 — curvez 기본형을 쓰지 않는다

curvez 규약의 토큰 이름은 `--<category>-<role>-<variant>` 이지만, 여기서는 **shadcn 이 정한 이름을
그대로 쓴다**(`--background`, `--muted-foreground` …).

**이유:** 이 이름들은 Tailwind 테마와 shadcn 컴포넌트가 직접 참조한다(`bg-background`,
`text-muted-foreground`). 이름을 바꾸면 `shadcn add` 로 받은 컴포넌트가 전부 동작하지 않아,
컴포넌트를 받을 때마다 손으로 고쳐야 한다. 규약의 의도(값-이름 금지, 의미로 짓기)는 shadcn 이름도
이미 지키고 있다 — `--muted-foreground` 는 역할 이름이지 색 이름이 아니다.

## 색

| 토큰                 | 라이트          | 다크                   | 용도                                                |
| -------------------- | --------------- | ---------------------- | --------------------------------------------------- |
| --background         | #F7F9F8         | #0B100F                | 화면 최하단 배경                                    |
| --foreground         | #14201E         | #E6EDEA                | 본문 텍스트. 랜딩의 --brand-ink 와 같은 값이다      |
| --card               | #FFFFFF         | #141A18                | 카드 배경. 캔버스보다 한 단계 밝아 면이 뜬다        |
| --card-foreground    | #14201E         | #E6EDEA                | 카드 안 본문                                        |
| --primary            | #14201E         | #E6EDEA                | 주요 CTA 배경                                       |
| --primary-foreground | #F7F9F8         | #0B100F                | 주요 CTA 라벨                                       |
| --muted              | #EDF1F0         | #1D2523                | 보조 배경(태그 칩, 코드 블록)                       |
| --muted-foreground   | #5C6B68         | #9DAAA6                | 보조 설명, 메타 정보                                |
| --accent             | #EDF1F0         | #1D2523                | hover 배경                                          |
| --accent-foreground  | #14201E         | #E6EDEA                | hover 상태의 텍스트                                 |
| --destructive        | #E7000B         | #FF6467                | 파괴적 행동. v1 화면에는 쓰이지 않는다              |
| --border             | #DDE3E1         | rgba(255,255,255,0.10) | 구분선·카드 테두리. 다크는 흰색 10% 알파다          |
| --ring               | #0B6B63         | #5EEAD4                | 포커스 링. --brand-accent 와 같은 값이다            |
| --radius             | 0.625rem (10px) | 0.625rem (10px)        | 반경 스케일의 기준값. 단계는 아래 `## 형태` 를 본다 |

**중립 스케일은 무채색이 아니라 브랜드 색조(green-grey)를 띤다.** shadcn 기본값을 그대로 쓰면
헤더 바(`--brand-canvas`)와 본문 배경이 서로 다른 팔레트로 읽혀 서브 페이지마다 이음매가 보인다.
명도는 기본값과 거의 같게 두고 채도만 얹었다.

**`--foreground` 와 `--brand-ink`, `--ring` 과 `--brand-accent` 는 값이 같다. 토큰은 합치지 않는다.**
**이유:** 하나는 사이트 전역의 역할이고 하나는 랜딩 전용이다. 지금 값이 같다고 합치면 랜딩 색을
조정할 때 서브 페이지 본문 색까지 함께 움직인다.

**다크의 `--border` 는 알파값이다.** hex 로 옮길 수 없어 rgba 로 적었다. 대비 검증에서 제외한다 —
알파 위 실제 색은 배경에 따라 달라져 고정 쌍으로 계산되지 않는다.

## 랜딩 팔레트 — 홈에만 쓴다

레퍼런스 포스터의 **느낌**(굵은 기하 무늬 · 단일 강조색 · 딱딱한 그리드)만 가져오고 색은 바꾼다.
라이트는 teal, 다크는 mint 다. 면과 무늬로는 홈(`/`)에서만 쓴다.

**상호작용 색은 사이트 전역에서 이 강조색 하나다.** 링크 hover 와 포커스 링이 전부 여기로 모인다.
**이유:** 화면마다 hover 색이 다르면 "여기를 누를 수 있다" 를 색으로 배울 수 없다. 강조색을
하나로 잠가야 나머지 색이 전부 중립으로 읽힌다.

| 토큰           | 라이트  | 다크    | 용도                                        |
| -------------- | ------- | ------- | ------------------------------------------- |
| --brand-accent | #0B6B63 | #5EEAD4 | 단일 강조색. 제목·무늬·채운 면              |
| --brand-canvas | #E4E8E7 | #101514 | 랜딩 바탕. shadcn 의 --background 와 다르다 |
| --brand-ink    | #14201E | #E6EDEA | 랜딩 본문·설명                              |

**강조색을 하나만 쓴다.** 두 번째 색을 들이지 않는다.
**이유:** 이 스타일이 성립하는 근거가 색 대비가 아니라 형태 대비(면·줄·원)다. 색이 둘이 되는 순간
무늬가 장식으로 읽히고, 강조가 어디에 있는지 판정할 수 없게 된다.

**무늬는 이미지가 아니라 CSS 그라디언트와 인라인 SVG 로 그린다.**
**이유:** 벡터라 어느 해상도에서도 선명하고 색이 토큰을 따라가 다크에서 자동으로 뒤집힌다.
비트맵으로 두면 테마마다 파일을 두 벌 관리해야 하고 그 둘이 조용히 어긋난다.

대비는 라이트 5.15 · 다크 12.46 로 **큰 글자 전용 제약이 없다.** 강조색을 본문에 써도 통과한다 —
다만 그렇게 쓰지 않는다. 위의 "강조색 하나" 규칙이 형태로 강조하라는 뜻이다.

## 간격·타이포는 새 토큰을 만들지 않는다

Tailwind 4 의 기본 스케일을 그대로 쓴다. 값을 고정할 자리만 아래로 못박는다.

| 대상           | 값                                          | 근거                               |
| -------------- | ------------------------------------------- | ---------------------------------- |
| 본문 폭        | `max-w-[68ch]`                              | 읽기 폭. 케이스 본문에만 적용한다  |
| 페이지 폭      | `max-w-5xl` (1024px)                        | 목록·홈의 바깥 컨테이너            |
| 섹션 세로 간격 | `py-16` (64px) / 모바일 `py-10` (40px)      | 4pt 그리드                         |
| 요소 간 간격   | `gap-2` / `gap-4` / `gap-8` 세 단계만       | 단계를 늘리면 판정 기준이 사라진다 |
| 제목           | `text-4xl` / `text-2xl` / `text-lg` 세 단계 | 화면 h1 / 섹션 h2 / 카드 제목      |
| 제목 자간      | h1 `-0.02em` · h2 `-0.015em` · 본문 0       | 큰 글자일수록 기본 자간이 벌어진다 |
| 본문 행간      | `leading-relaxed` (1.625)                   | 한글 본문 기준                     |

**h1 은 어느 화면에서도 `text-4xl` 이다.** 랜딩 워드마크만 예외로 `clamp()` 를 쓴다 — 그것은
제목이 아니라 브랜드 마크다.

**이유:** 한 곳에서만 쓰는 값을 토큰으로 올리면 이름만 늘고 의미는 안 는다. 세 곳 이상에서
같은 값이 필요해지는 순간 그때 승격한다.

## 형태 — 반경 스케일 하나만 쓴다

한 화면에 둥근 버튼과 각진 카드가 섞이면 어느 쪽이 의도인지 판정할 수 없다. 단계를 셋으로 못박는다.

| 대상                           | 클래스       | 값                |
| ------------------------------ | ------------ | ----------------- |
| 카드 · 코드 블록               | `rounded-lg` | 10px (`--radius`) |
| 아이콘 버튼 · 포커스 링 모서리 | `rounded-md` | 8px               |
| 태그 칩 · 인라인 코드          | `rounded-sm` | 6px               |

**스케일 밖의 `rounded`(4px)를 쓰지 않는다.** Tailwind 기본값이라 손에 먼저 잡히지만
`--radius` 를 따라가지 않아 반경을 조정할 때 혼자 남는다.

## 대비 검증

- fg=#14201E bg=#F7F9F8 mode=light min=4.5 # 본문·h1/캔버스 → 15.82
- fg=#5C6B68 bg=#F7F9F8 mode=light min=4.5 # 보조 설명·푸터/캔버스 → 5.28
- fg=#14201E bg=#FFFFFF mode=light min=4.5 # 카드 제목/카드 → 16.73
- fg=#5C6B68 bg=#FFFFFF mode=light min=4.5 # 카드 요약·날짜/카드 → 5.59
- fg=#5C6B68 bg=#EDF1F0 mode=light min=4.5 # 칩 글자/칩 배경 → 4.91
- fg=#0B6B63 bg=#F7F9F8 mode=light min=4.5 # 링크 hover·강조/캔버스 → 6.02
- fg=#0B6B63 bg=#FFFFFF mode=light min=4.5 # 링크 hover·강조/카드 → 6.37
- fg=#F7F9F8 bg=#14201E mode=light min=4.5 # CTA 라벨/CTA 배경 → 15.82
- fg=#E6EDEA bg=#0B100F mode=dark min=4.5 # 본문·h1/캔버스 → 16.13
- fg=#9DAAA6 bg=#0B100F mode=dark min=4.5 # 보조 설명·푸터/캔버스 → 7.97
- fg=#E6EDEA bg=#141A18 mode=dark min=4.5 # 카드 제목/카드 → 14.84
- fg=#9DAAA6 bg=#141A18 mode=dark min=4.5 # 카드 요약·날짜/카드 → 7.34
- fg=#9DAAA6 bg=#1D2523 mode=dark min=4.5 # 칩 글자/칩 배경 → 6.51
- fg=#5EEAD4 bg=#0B100F mode=dark min=4.5 # 링크 hover·강조/캔버스 → 12.96
- fg=#5EEAD4 bg=#141A18 mode=dark min=4.5 # 링크 hover·강조/카드 → 11.92
- fg=#0B100F bg=#E6EDEA mode=dark min=4.5 # CTA 라벨/CTA 배경 → 16.13
- fg=#14201E bg=#E4E8E7 mode=light min=4.5 # 헤더 바 본문·워드마크/바 → 13.54
- fg=#0B6B63 bg=#E4E8E7 mode=light min=4.5 # 헤더 바 hover/바 → 5.15
- fg=#E6EDEA bg=#101514 mode=dark min=4.5 # 헤더 바 본문·워드마크/바 → 15.50
- fg=#5EEAD4 bg=#101514 mode=dark min=4.5 # 헤더 바 hover/바 → 12.46

**화살표 뒤 수치는 렌더된 화면에서 잰 값이다.** 소스 값이 아니라 브라우저가 실제로 계산한
`getComputedStyle` 색을 sRGB 로 바꿔 계산했다. 소스만 보면 상속·알파 때문에 실제와 갈린다.

**면과 면의 구분(카드/캔버스, 헤더 바/캔버스)은 이 목록에 넣지 않는다.** 라이트 1.06·1.17,
다크 1.09·1.04 로 대비 기준을 만족하지 않지만, 그것을 가르는 것은 색이 아니라 테두리 1px 이다.
글자 대비 기준을 면 구분에 적용하면 통과할 수 없는 검사를 만든다.
