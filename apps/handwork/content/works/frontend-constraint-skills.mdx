export const meta = {
  title: "프론트엔드 코드의 디자인 규칙을 검사하는 스킬",
  summary: "하드코딩된 색상을 찾아 시맨틱 토큰으로 바꾸고 색상 대비를 검사한다. 현재는 curvez로 옮겨 원래 저장소를 아카이브했다.",
  date: "2026-09",
  role: "개발 도구",
  link: "https://github.com/knut15/frontend-constraint-skills",
  group: "tool",
  tags: ["claude-code", "design-tokens", "accessibility", "lint"],
};

## 1. 작업 소개

프론트엔드 코드가 프로젝트 규칙을 지키는지 검사하는 스킬이다. 위반을 찾는 결정론적 스크립트와 모델의 작업 절차를 설명한 문서를 함께 제공한다. 검사 결과에는 수정할 위치가 나온다.

## 2. 구현 과정과 선택 기준

하드코딩된 색을 찾아 시맨틱 토큰에 매핑하고, 바꾼 뒤 모든 라이트·다크 토큰 쌍을 WCAG 대비로 검증한다.

한계를 README 에 적었다. shadcn 이 아닌 프로젝트에서도 스캐너는 돌지만, 대비 검사기는 `--x-foreground` 명명 규약으로 짝을 짓기 때문에 통과 건수가 0 으로 나온다.

## 3. 현재 상태와 남은 작업

**아카이브했다.** curvez 의 `plugins/frontend-constraints` 로 옮겼고 이 저장소는 더 갱신하지 않는다. 예고했던 패키지 경계 검사와 공개 API 표면 검사는 미구현으로 남았다.
