import { Steps } from "@scopulus/ui";
import { Example, Props } from "@/widgets/example";

<Props
  rows={[
    {
      name: "steps",
      type: "string[]",
      required: true,
      note: "순서가 곧 화면 순서다",
    },
    {
      name: "current",
      type: "number",
      required: true,
      note: "지금 있는 단계의 0부터 센 자리",
    },
  ]}
/>

<Example
  title="Steps"
  center={false}
  code={`<Steps steps={["요구 정리", "화면 설계", "구현", "배포"]} current={1} />`}
>
  <Steps steps={["요구 정리", "화면 설계", "구현", "배포"]} current={1} />
</Example>

<Example
  title="First step"
  center={false}
  code={`<Steps steps={["요구 정리", "화면 설계", "구현", "배포"]} current={0} />`}
>
  <Steps steps={["요구 정리", "화면 설계", "구현", "배포"]} current={0} />
</Example>

<Example
  title="Last step"
  center={false}
  code={`<Steps steps={["요구 정리", "화면 설계", "구현", "배포"]} current={3} />`}
>
  <Steps steps={["요구 정리", "화면 설계", "구현", "배포"]} current={3} />
</Example>

<Example
  title="All done"
  center={false}
  code={`<Steps steps={["요구 정리", "화면 설계", "구현", "배포"]} current={4} />`}
>
  <Steps steps={["요구 정리", "화면 설계", "구현", "배포"]} current={4} />
</Example>

<Example
  title="Two steps"
  center={false}
  code={`<Steps steps={["작성", "발행"]} current={0} />`}
>
  <Steps steps={["작성", "발행"]} current={0} />
</Example>
