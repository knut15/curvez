import { AppLink, ThemeToggle } from "@scopulus/ui";
import { Example, Props } from "@/widgets/example";

<Props
  rows={[
    {
      name: "없음",
      type: "—",
      note: "상태를 스스로 읽고 쓴다. 부모가 넘길 값이 없다",
    },
  ]}
/>

<Example title="ThemeToggle" code={`<ThemeToggle />`}>
  <ThemeToggle />
</Example>

<Example
  title="In a header"
  code={`<nav className="flex items-center gap-4">
  <AppLink href="/components" variant="bare">Components</AppLink>
  <ThemeToggle />
</nav>`}
>
  <nav className="flex items-center gap-4">
    <AppLink href="/components" variant="bare">
      Components
    </AppLink>
    <ThemeToggle />
  </nav>
</Example>
